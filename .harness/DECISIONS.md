# DECISIONS — backend-discovery

최신 결정이 표 최상단에 온다 (append-only).

| 날짜 | 결정 | 이유 |
| --- | --- | --- |
| 2026-09-02 | 도서 표준 장르 분류 API(`POST /api/v1/classify-genre`)의 요청 스키마(`BookClassificationRequest`)를 `isbn: str` 단일 필드로 간소화하고, 기존 `title`, `author`, `raw_category` 필드를 제거한다. 도메인 프롬프트 및 서비스 로직도 도서 고유 식별자인 ISBN만을 기반으로 16개 표준 장르 분류를 수행하도록 단일화한다 (CLIAR-235) | 도서 등록 및 OCR 파이프라인에서 도서 식별과 조회가 고유 식별자인 ISBN 기반으로 개편됨에 따라, 장르 분류 API에서도 불필요한 메타데이터 필드(제목, 저자, 원본 카테고리) 전송을 없애고 요청 페이로드를 단일화했다. ISBN만으로 LLM이 도서를 특정하고 ERD 표준 16개 장르 체계로 정확하게 분류할 수 있으며, 호출자(`backend-book`)의 요청 생성 복잡도와 네트워크 페이로드를 최소화할 수 있다 |
| 2026-09-02 | (1) Tavily 검색 도구(`search_books`)에서 `raw_content` 등 불필요한 거대 필드를 제거하고 `content`를 400자로 슬라이싱하는 `sanitize_search_results` 순수 함수를 도입한다. (2) 오케스트레이터 프롬프트에서 도서 마크다운 카드 재생성을 금지하고 간결한 서두 멘트(1~2줄)만 생성하도록 축소하여, 기존 서비스 레이어(`orchestrator_service.py`)의 도구 결과 결합(`tool_result`, `### 📖`, `### 📚`) 로직을 온전히 활용한다. (3) 리전 롤백 없이 Sonnet 5 글로벌 프로필을 유지하며 추론 파라미터로 `temperature=0.5`, `top_p=0.9` 및 `max_tokens` 최적화(오케스트레이터 1024, 추천 에이전트 1536)를 적용한다 (CLIAR-171) | 실측 분석 결과(총 40.4초 중 추천 에이전트 23.8초, 59% 차지), Tavily의 원본 웹페이지 크롤링 결과(`raw_content`)가 가공 없이 LLM에 들어가 16,769개의 거대한 입력 토큰을 유발하고 추론 시간을 21초까지 지연시키던 최대 병목이 확인되었다. 페이로드를 핵심 서지 정보(400자 상한)로 정제하여 입력 토큰을 90% 이상 절감했다. 또한 오케스트레이터가 하위 에이전트가 완성한 도서 카드를 본문에서 다시 생성(840 출력 토큰)하던 중복을 제거하고 기존 서비스 결합 로직에 위임하여 오케스트레이터 출력 시간을 1초대로 단축했다. 아울러 `temperature=0.5`와 `top_p=0.9`를 적용하여 환각을 억제하면서도 정형화된 추천 마크다운을 안정적으로 생성하도록 유도했다 |
| 2026-09-02 | 위기 및 자해 대응(`domain/orchestrator/safety_gate.py`)에 대해 LLM 추론을 거치지 않고 공인 위기상담전화(109 등) 핫라인을 즉각 반환하는 **결정론적 안전 게이트(Safety Gate)**를 도입한다. 이는 CLIAR-208의 "하드코딩 응답 지양 / LLM 유연한 대화" 원칙에 대한 **명시적 안전 예외**로 규정한다 | 사용자 안전 및 생명 보호 프로토콜은 LLM의 확률적 생성, 환각(Hallucination), 또는 프롬프트 우회(탈옥) 위험을 100% 원천 차단해야 한다. LLM이 생성하는 공감 멘트만으로는 위기 상황에 처한 사용자에게 필수적인 24시간 상담 핫라인(109, 1577-0199, 1588-9191) 안내가 누락될 수 있음이 QA 46건 실측에서 확인되었다. 순수 함수 기반 결정론적 안전 게이트는 0ms TTFB, 0 토큰 비용으로 즉각적인 위기 대응 프로토콜을 보장하며, 안전/생명 관련 사안은 LLM 추론보다 결정론적 규칙이 항상 우선한다 |
| 2026-09-01 | `/api/v1/chat`의 인증을 discovery 자체 JWT 서명 검증(Option A)도 순수 게이트웨이 신뢰(Option B 원안)도 아닌 **헤더 존재 검증(Presence Check)**으로 결정한다. `Authorization` 헤더가 없거나 빈 문자열이면 discovery가 401을 직접 반환하고, 서명/만료의 실질 검증은 `backend-book` 호출 시점의 401을 그대로 전달한다 | `k8s/base/ingress.yaml`을 직접 확인한 결과 ALB에 인증 어노테이션이 전혀 없어 "게이트웨이가 이미 검증했다"는 Option B의 전제가 거짓임이 확인됐다(게이트웨이 자체가 인증을 안 함). 반대로 Option A는 discovery에 JWT 시크릿/공개키/JWKS 연동이 전혀 없어 새로 구축해야 하고, 이는 별도 인증 서비스와의 계약 수립이 필요한 범위 확장이라 CLIAR-215에 맞지 않는다. 헤더 존재 검증은 QA 18번(헤더 없음)을 즉시 해소하고, `library_tool.py`가 `auth_token` 없을 때 조용히 빈 리스트를 반환하던 문제도 라우터 단계에서 먼저 걸러 해소한다. 서명 검증(QA 19번, 위조 JWT)은 `backend-book`의 401을 그대로 전달하는 것으로 대체하며, 진짜 서명 검증이 필요해지면 게이트웨이 승격 또는 discovery 자체 검증을 백로그로 재검토한다. 이 프로젝트에 이미 있는 `/internal/*` 라우터의 공유 시크릿 검증 전례(2026-08-19 결정)와 같은 방향이다 |
| 2026-09-01 | `CLIAR-158-Strands-Agent-Optimization` 브랜치(로컬+원격)를 develop 병합 없이 폐기(삭제)한다. 이 브랜치는 "Strands SDK 기반 고도화" 방향으로 별도 세션이 독자적으로 착수해 "직결 스트리밍(Direct Streaming Pipeline)을 `asyncio.Queue` 기반 바이패스로 구현 완료"라고 표기했었다 | 이 접근은 이번 세션이 Strands 1.26 SDK 소스(`@tool` 함수가 `str`을 반환하는 계약)를 직접 확인해 하위 에이전트 스트림을 상위 스트림에 직결 바이패스하는 것이 아키텍처적으로 불가능하다고 판정하고 백로그로 이관한 것과 정면으로 배치된다. 실제로 이 브랜치는 이번에 검증·머지된 CLIAR-158(계측 + `consult` 중복 제거 + 캐싱 배선, 커밋 `35394cb`/`8039d99`/`f59409f`/`5918a2d`)과 다른 시점(`f8c1c54`, CLIAR-170)에서 분기한 별개 작업물이며, `origin/develop`과 병합 시 `orchestrator_service.py` 등에서 20개 이상 충돌이 발생함을 `git merge-tree`로 확인했다. "Strands 기반 고도화"라는 접근 자체를 이제 쓰지 않기로 하여(직결 스트리밍은 백로그, 계측·순손실 제거는 이미 다른 방식으로 완료됨) 재작업 없이 폐기한다. 함께 확인한 `CLIAR-111-Librarian-Agent-Integration`/`CLIAR-152-Unified-Agent-Assistant`의 미머지 커밋(시리즈 도서 풀네임 보존 프롬프트)도 CLIAR-91 재작성 시점에 동일 내용이 이미 develop에 흡수되어 있음을 대조 확인한 뒤 삭제했다 |
| 2026-09-01 | CLIAR-158을 통해 (1) `core/observability.py` 구조화 레이턴시/토큰 계측 도입 및 `tool_usage[*].tool_info.input_params` 화이트리스트 필터링으로 개인정보 방어, (2) `consult_librarian` 도구 중복 호출 제거(tail 조건식 버그 `not switch_to_holder`를 `not consult_called`로 수정, prefetch 결과 1회차 재사용, 스트리밍 라우터 signals fallback 및 동기 tail 타임아웃 보존), (3) `Settings.enable_prompt_caching` 런타임 배선(기본값 `False` 유지, dev 배포 후 실측하여 활성화 판단)을 적용 | 기존 `stream_chat`에서 최대 3회(초기 메타 + LLM 도구 + tail) 발생하던 원격 사서 호출을 1회로 최적화하고, `initial_meta_timeout_seconds` 가드를 스트리밍 tail 경로에 적용해 제너레이터 지연을 차단했다. 또한 개인정보 보호를 위해 사용자 원문은 물론 도구 인자(`input_params`)까지 철저히 필터링하고, 프롬프트 캐싱은 실측 없이 기본 활성화하지 않고 `False`로 안전하게 둔 뒤 dev 실측 로그 기반으로 판단하도록 설계했다. |
| 2026-09-01 | 오케스트레이터 개선 작업의 진행 순서를 **CLIAR-158 → CLIAR-215 → CLIAR-171 → CLIAR-216**으로 확정하고, 기존 CLIAR-215(QA 데이터셋 기반 고도화)를 프롬프트를 건드리지 않는 CLIAR-215(안전성·인증·입력 게이트)와 프롬프트를 확장하는 CLIAR-216(가드레일 프롬프트 고도화) 두 티켓으로 분할한다. 프롬프트를 **축소하는 작업(CLIAR-171)이 확장하는 작업(CLIAR-216)보다 먼저** 수행된다 | CLIAR-171과 CLIAR-216이 `domain/orchestrator/agent.py`의 같은 페르소나 프롬프트 상수를 수정하므로 병행하면 충돌한다. 순서를 뒤집으면 (1) CLIAR-216이 작성한 출력 구조 지침 일부가 CLIAR-171의 카드 재출력 규칙 제거로 무의미해져 재작업이 발생하고, (2) 프롬프트가 가장 비대한 상태(cat 2,635자 / stork 2,700자, CLIAR-208 → 211 → 213에서 3회 연속 지침 추가)에서 구조 변경을 하게 되어 회귀 원인을 분리할 수 없다. CLIAR-171이 먼저 줄여주면 CLIAR-216의 `SHARED_GUARDRAILS` 공통 규칙 분리도 축소된 프롬프트 기준으로 설계할 수 있다. CLIAR-158을 최우선에 두는 이유는 충돌 대상이 없는 순손실 제거(요청당 `consult` 최대 3회 호출, 프롬프트 캐싱 런타임 미적용)이면서 계측 기반이 이후 티켓의 정량 판단 근거가 되기 때문이다. CLIAR-215를 통째로 마지막에 두면 P1 안전성(자해 위기 대응)과 인증 공백이 가장 늦게 처리되는데, 두 항목의 구현 위치가 입력 게이트 코드와 `api/deps.py`라 프롬프트와 충돌하지 않으므로 분리해 앞으로 당겼다. 티켓을 위험도 축(순손실 제거 → 계약 무변경 튜닝 → 프롬프트 확장)으로 나눈 것은, 원래 제안이었던 기술 축(Strands 트랙 / Bedrock 트랙) 분할이 1줄 변경(캐싱 활성화)과 아키텍처 변경(직결 스트리밍)을 같은 티켓에 묶어 회귀 원인 추적을 불가능하게 만들기 때문이다 |
| 2026-08-28 | 기존 서재 CRUD 마이크로서비스(`backend-book`)의 `GET /api/v1/library/books` API를 호출하는 HTTP 프록시 도구(`SearchMyLibraryTool`)를 구현하고, 클라이언트 `Authorization` Bearer 토큰을 서비스 레이어 클로저로 주입하여 오케스트레이터의 내 서재 검색 및 복합 도서 추천을 완성 (ADR 0004) | 사용자가 프론트엔드에서 수동으로 검색/추천 모드를 선택할 필요 없이, 단일 자연어 입력창에서 내 서재 보유 여부, 독서 상태, 진행률 확인 및 내 서재 도서 기반 연계 추천을 수행하는 Zero-Friction 올인원 독서 비서 아키텍처를 완성하기 위함이다. 서재 데이터를 Discovery가 자체 복제하지 않고 기존 API를 HTTP 프록시 도구로 호출하며, 인증 토큰은 LLM이 인자로 다루지 않고 서버 클로저로 직접 주입하여 IDOR 취약점을 원천 방어했다. |
| 2026-08-27 | 추천 에이전트 및 오케스트레이터 기본 Bedrock 모델을 Claude 3 Haiku에서 Claude 3.5 Sonnet v1(`anthropic.claude-3-5-sonnet-20240620-v1:0`, 서울 `ap-northeast-2`)으로 업그레이드하고, 최신 CRIS Reasoning 모델들(Sonnet 4/4.5/5 등)은 실시간 대화 스트리밍이 아닌 비동기/배치/장르 분류용 백로그로 분리 | AWS 교육 계정의 Claude 모델 권한이 해제됨에 따라 모델 선정을 재검토했다. 최신 CRIS 모델들은 reasoning 오버헤드로 TTFT가 1.7~2.4초에 달하고 reasoningContent 블록 파싱 처리가 필요해 실시간 챗봇 대화에는 부적합하다. 반면 서울 리전(ap-northeast-2)의 Claude 3.5 Sonnet v1은 실측 TTFT 617ms로 빠르고 정확도(21/21)가 검증되었으며, 사서 에이전트(`backend-librarian`)와 동일한 모델을 사용해 도구 연쇄 호출(Chaining)과 프롬프트 지침 준수율을 극대화할 수 있다. |
| 2026-08-27 | 모든 PR/병합 대상을 `develop`으로 고정한다. `develop → main` 릴리스 병합은 별도 지시가 있을 때까지 진행하지 않으며, `main`에 직접 PR·커밋·병합하는 예외를 두지 않는다. 이에 맞춰 `develop`에 머지 완료된 피처 브랜치(`CLIAR-102/103×2/114/51/67/86/91`) 8개를 로컬·원격에서 일괄 정리했다(머지 완료는 `git merge-base --is-ancestor`로 확인 후 진행) | `main`이 `develop`보다 20커밋 behind인 상태가 지속되고 있어, 릴리스 병합 시점과 방식을 별도로 논의하기 전까지는 `develop`을 유일한 통합 브랜치로 유지하는 게 혼선을 줄인다. 브랜치 정리는 머지 완료 후 방치하면 로컬/원격에 죽은 브랜치가 계속 쌓여 다음 세션에서 어떤 브랜치가 유효한지 판단하는 비용이 커지므로, 머지 완료가 확인된 브랜치는 즉시 정리하는 것으로 `AGENTS.md`에 명문화했다. |
| 2026-08-27 | 사서 에이전트(`backend-librarian`) 연동에서 (1) signals→추천 반영은 서비스 레벨 강제 결합이 아니라 오케스트레이터 프롬프트 레벨 위임으로 구현, (2) 로컬 개발 포트 충돌은 discovery 쪽을 8001로 이동(사서 쪽 8000 유지), (3) 위치 좌표(`latitude`/`longitude`)는 세션 시작(로그인) 시점에 1회만 수신해 세션 메타로 캐시하고 이후 턴은 재사용하며 재로그인(신규 세션) 시 재수신, (4) `switch_to`는 텍스트 안내가 아니라 `ChatResponse`에 구조화된 옵셔널 필드로 그대로 통과시켜 프론트가 사서 전환 시 테마를 구조적으로 갱신할 수 있게 함, (5) 사서 HTTP 호출 타임아웃(현재 10초 하드코딩)은 사서 팀 실측 확인 전까지 임시 20초로 상향하고 환경변수(`LIBRARIAN_HTTP_TIMEOUT_SECONDS`)로 분리, (6) 추천/오케스트레이터/장르 분류 모델의 Claude 3 Haiku → Sonnet 3.5 통일은 이번 사서 연동 범위에서 제외하고 별도 작업으로 분리 | signals 반영을 서비스가 강제 결합하면 8/25에 지적한 "위임이 아니라 마이크로매니징" 문제를 다시 만든다 — Agents-as-Tools 아키텍처와 정합적으로 LLM 판단에 맡기는 방향을 우선 시도한다. 포트는 팀원 레포(`backend-librarian`)를 바꾸는 것보다 discovery 쪽 `docker-compose.yml` 포트 매핑만 바꾸는 게 변경 범위가 작다. 좌표를 매 턴 받으면 불필요한 트래픽과 프론트 로직 복잡도가 늘고, `ChatSessionStore`의 기존 세션 생명주기(TTL)에 얹으면 별도 만료 로직 없이 자연스럽게 갱신된다. `switch_to`를 텍스트로만 안내하면 프론트가 이미 갖고 있는 사서별 테마 전환 UI를 구조적으로 트리거할 수 없다. 타임아웃 10초는 근거 없는 임의값이고 Sonnet 3.5 + 도구 호출을 쓰는 사서에는 부족할 수 있어, 사서가 정상 동작 중에도 "서비스 준비 중" 오탐 fallback이 나가는 문제를 방지해야 한다. 모델 통일은 비용·레이턴시 영향이 크고 CLIAR-91/114에서 Haiku 기준으로 검증된 마크다운/번역 지침의 회귀 위험이 있어, 대규모 변경 시 독립적인 축을 같은 브랜치에 섞지 않는다는 원칙에 따라 분리한다(`.harness/BACKLOG.md`에 별도 항목으로 기록). |
| 2026-08-26 | 추천 에이전트 시스템 프롬프트에 해외 도서(일본어/영어 등)의 제목·권차·저자명을 한국어 표준 명칭으로 번역하는 지침을 추가하고, 상시 톤앤매너 규칙(과잉 사과 지양 + 다정한 인사말/마무리)을 압축 통합하여 총 7개 규칙 체계로 정돈 | 별도 번역 에이전트 도구(B안)는 호출 판단 자체가 추론 비용을 유발하고 레이턴시가 폭증하여 기각했다. C안(정규식 감지 + Haiku 단발 후처리)은 아직 실사용에서 외국어 잔존 사례가 관찰되지 않아 백로그로 유예하고, 추가 LLM 호출이나 지연시간 증가가 없는 시스템 프롬프트 지침 추가(A안)를 채택했다. 또한 추천 전용 경로(단독 호출)의 상시 톤앤매너를 지키면서 전체 프롬프트 길이와 규칙 수를 7개로 컴팩트하게 유지하기 위해 기존 사과 방지와 인사말 규칙을 하나의 톤앤매너 규칙으로 압축했다. |
| 2026-08-25 | 도서 추천 도구(`RecommendBooksTool`)에 `count: int = 1` 인자 구조화와 프롬프트 주입을 통한 생성량 유도 및 도메인 순수 함수(`truncate_books_by_count`)를 통한 상한 강제를 결합하고, 서지 메타데이터(쪽수) 수집을 강화 | 프롬프트만으로는 LLM의 권수 규칙 준수를 100% 보장할 수 없으므로, 생성량 유도(하한 방어)와 정규식 파서 기반 마크다운 후처리 자르기(상한 강제)를 결합하여 결정론적 수량 보장을 완성했다. 또한 프론트엔드의 300쪽 fallback 현상을 해소하기 위해 `- **저자**: {저자명} ({페이지수}쪽)` 표준 규격과 서지 검색 힌트를 추가하고 불필요한 과잉 사과를 제거했다. |
| 2026-08-25 | 도서 장르 분류를 위한 경량 REST API (`POST /api/v1/classify-genre`)를 신설하고, ERD 16개 표준 장르 Enum(`StandardGenre`) 및 Claude 3 Haiku 제로샷 프롬프트 + 견고한 fallback 파서(`parse_classification_response`)를 적용 | OCR 및 외부 알라딘 도서 메타데이터(비정형 원본 카테고리, 제목, 저자)를 DPYB 서비스의 ERD 16개 표준 장르 규격으로 정제·매핑하기 위한 전용 API가 필요했다. 오케스트레이터 대화 세션과 무관한 경량 단일 턴 호출 구조로 설계하고, LLM 출력의 비정형성이나 일시 장애 시 500 에러 대신 '기타'(`ETC`) 장르와 confidence 0.0으로 graceful fallback하도록 구현했다 (`docs/api/decisions/0002-book-genre-classification.md` 참고). |
| 2026-08-24 | 오케스트레이터 에이전트(Strands Agent-as-a-Tool)를 상위 진입점으로 두고, 도서 추천은 로컬 도구(`RecommendBooksTool`), 사서 상담은 HTTP 스텁 도구(`ConsultLibrarianTool`)로 라우팅하는 2계층 에이전트 아키텍처 채택 | 단순 도서 검색/추천과 페르소나 기반 사서 상담의 역할을 분리하고, 향후 사서 에이전트 마이크로서비스(`backend-librarian`) 독립 배포 시 API 라우팅 변경 없이 원격 도구 호출로 자연스럽게 전환할 수 있도록 설계했다. 오케스트레이터 모델이 도구 결과(추천 도서 카드 마크다운) 생략 시 본문에 자동 결합하는 결정론적 fallback 안전장치(`extract_fallback_text`)를 추가했다. |
| --- | --- | --- |
| 2026-08-21 | 추천 에이전트의 Bedrock 모델을 Claude 3 Haiku(`anthropic.claude-3-haiku-20240307-v1:0`)로 확정한다(조사 시점 1차 추천이었던 Claude Haiku 4.5 대신). `core/config.py`의 `Settings`에 모델 ID를 용도별 필드(`librarian_model_id`)로 분리해, 나중에 특정 기능만 다른 모델(예: Claude 3.5 Sonnet)로 바꿀 때 해당 필드만 교체하면 되도록 설계했다 | 실제 계정으로 콘솔+CLI 호출을 확인한 결과 Claude Haiku 4.5/Claude Sonnet 4 이상 최신 모델은 `kosa-edu-region-pol`로 전 리전 차단되어 있고, Claude 3 Haiku와 Claude 3.5 Sonnet은 호출 가능함이 확인됐다(`.harness/BACKLOG.md` 참고). 단일 `bedrock_model_id` 필드로 전체 서비스를 통일하면 향후 특정 기능(예: 더 복잡한 큐레이션 로직)만 Sonnet으로 업그레이드하려 할 때 다른 용도의 모델까지 함께 바뀌는 문제가 생긴다. 용도별 필드로 미리 분리해 이 문제를 예방한다. |
| 2026-08-21 | backend-discovery 레포는 폐기하지 않고 존속한다. 자연어 질의 기반 도서 추천 기능도 별도 레포로 이관하지 않고 backend-discovery 자체를 "추천 에이전트"로 재설계한다. 이전 결정(별도 레포로 이관)을 뒤집는다. PostgreSQL/pgvector/SQLAlchemy(async)/asyncpg/Alembic/testcontainers(postgres)를 의존성·코드·인프라 설정에서 완전히 제거한다(RDB로 남는 데이터가 전혀 없어서). 웹 검색 도구는 Tavily로 확정한다(Strands 생태계 공식 파트너, LLM 에이전트용으로 결과가 사전 정제되어 옴) | 세션 스토어(`ChatSessionStore`)까지 별도 레포로 넘기면 backend-discovery에 남는 로직이 없어진다는 문제가 제기되어 레포 존속 여부를 재검토했다. 검토 결과, 애초에 별도 레포로 이관할 필요 없이 backend-discovery 자체가 그 역할(추천 에이전트)을 계속 맡는 것으로 방향을 재확정했다. RDB 제거는 8단계 조사에서 "지금 남는 RDB 테이블이 전혀 없다"는 결론이 나온 데 따른 후속 결정이다. Tavily는 Strands 공식 블로그가 기여사로 명시하고 있어 SDK와의 호환성이 검증된 선택지다. |
| 2026-08-21 | 벡터DB(pgvector) 기반 자체 벡터 인덱스와 그에 기반한 검색을 backend-discovery에서 전부 폐기한다. **자연어 질의 기반 도서 추천 기능 자체는 폐기가 아니라 재구현이다** — 웹 검색 도구를 사용하는 Strands 에이전트로 대체된다. ~~추천 기능은 별도 레포로 이관한다.~~ **[2026-08-21 후속 결정으로 정정]** 별도 레포 이관은 취소되고 backend-discovery 자체가 추천 에이전트로 재설계된다(위 최신 결정 참고). backend-discovery는 `ChatSessionStore`(Redis 기반 대화 세션 관리)를 계속 사용한다 | S3 Vectors 조사(`.harness/research/2026-08-21-s3-vectors-review.md`) 결과 현재 규모(25만 건)·쿼리 패턴(높은 빈도)·하이브리드 검색 요구사항 모두 S3 Vectors의 최적 조건과 반대 방향이라 전환 이점이 없다고 판단했다. 별개로 Strands Agents SDK 도입 조사(`.harness/research/2026-08-21-strands-agents-poc-design.md`)를 진행하며 "추천 에이전트는 공용, 페르소나는 에이전트별 분리, 향후 날씨/시간대/테마 큐레이션까지 에이전트가 담당"하는 확장 가능한 구조가 필요하다는 목표가 확인됐다. 이때 우리 DB의 벡터 인덱스로 도서를 찾는 대신, 에이전트가 웹 검색 도구로 실시간 조회해 추천하는 방식으로 구현 방식이 바뀌는 것이며, "질문하면 추천해준다"는 기능 자체는 유지된다. 폐기된 코드는 `archive/vector-search-poc/`에 원래 경로 구조를 유지한 채 보관해 참조할 수 있게 했다. |
| 2026-08-21 | `POST /internal/sync-book`(단건 동기 HTTP)은 실시간 갱신·테스트용으로 범위를 좁히고, 대량 초기 적재는 별도 CSV 배치로 처리한다. Basic API 담당자 확인: 하루 최대 5,000건, 총 약 25만 건 규모이며 25만 건 CSV 원본 파일은 이미 전달받은 상태 | 25만 건 규모를 단건 HTTP 호출로 적재하면 요청 수·지연시간·부분 실패 처리 비용이 커진다. CSV 배치는 대량 적재에 적합한 방식이고, 단건 동기 API는 Basic API 쪽에서 개별 도서가 생성/수정될 때의 실시간 반영과 통합 테스트 용도로 남겨둔다. CSV 배치 자체(동시성 제어, 실패 재시도, 멱등성)는 별도 티켓으로 분리한다(`.harness/BACKLOG.md` 참고). **[2026-08-21 후속 결정으로 폐기]** 벡터DB/RAG 기반 카탈로그 검색 자체가 폐기되어 이 결정과 관련 ADR(`docs/api/decisions/0001-internal-sync-contract.md`)도 `archive/vector-search-poc/`로 이동했다. |
| 2026-08-21 | `redis.asyncio.Redis`의 메서드 반환값은 mypy override(`ignore_missing_imports`)가 아니라 `typing.cast`로 명시적으로 처리 | `redis`는 `pgvector`/`testcontainers`/`boto3`와 달리 `py.typed` 마커가 있어 실제로 타입이 분석된다. 다만 redis-py가 동기/비동기 클라이언트를 같은 시그니처로 오버로드해 반환형이 `Awaitable[T] | T`로 애매하게 잡힌다. override로 모듈 전체를 무시하면 실제 타입 에러까지 놓칠 수 있어, 호출부마다 `cast`로 async 반환형만 명시했다. |
| 2026-08-21 | 대화 세션 TTL은 고정 만료가 아니라 매 쓰기(`append_turn`)마다 갱신되는 sliding window 방식으로 구현 | 사용자가 대화를 계속 이어가는 도중에 세션이 만료되어 문맥이 끊기면 챗봇 경험이 나빠진다. 마지막 턴 이후 일정 시간(기본 1시간, `CHAT_SESSION_TTL_SECONDS`) 동안 추가 메시지가 없을 때만 만료되게 해, 활성 대화는 계속 유지하면서 방치된 세션은 자동 정리한다. |
| 2026-08-21 | `mypy-boto3-bedrock-runtime` 스텁을 dev 의존성에 추가하고, `[[tool.mypy.overrides]]`에 `boto3.*` (`ignore_missing_imports = true`)도 함께 추가 | mypy strict에서 `import boto3` 자체가 py.typed 마커 없음 에러(`import-untyped`)를 냈다. `mypy-boto3-bedrock-runtime`은 `BedrockRuntimeClient` 같은 반환 타입 어노테이션에는 유용하지만 `boto3` 패키지 자체의 타입 스텁을 제공하지 않아 이 에러를 해소하지 못했다. `pgvector`/`testcontainers`와 동일하게 `boto3.*`를 override 대상에 추가해 해결했다. |
| 2026-08-21 | `AGENTS.md`에 "Git 작업 정책" 섹션 추가(커밋은 Task 단위+`[CLIAR-XX]` 태그 필수, push/merge는 사용자 승인 없이 금지, push 전 변경 파일·diff 요약 선제시) + `.pre-commit-config.yaml`로 ruff/mypy/커밋 메시지 형식 훅 도입 | CLIAR-40부터 커밋 단위를 세분화하고 push/merge 실수를 원천 방지하기 위해 명문화가 필요했다. pre-commit 훅으로 커밋 시점에 정적 분석과 메시지 형식을 자동 검증해 사람이 매번 수동으로 챙기지 않게 한다. push/merge 승인 정책은 훅이 아니라 에이전트 워크플로우 규칙이므로 pre-commit 범위에는 포함하지 않는다. |
| 2026-08-21 | AWS 계정 확보 이후에도 Bedrock 실연동 스위치는 기존 `LLM_PROVIDER=mock\|bedrock`(`core/config.py`)를 그대로 사용하고, 별도 `USE_REAL_BEDROCK` 같은 boolean 플래그는 추가하지 않는다. 기본값은 `mock` 유지 | `LLM_PROVIDER`가 이미 구현체 선택이라는 동일한 역할을 하고 있어, 별도 스위치를 추가하면 두 값이 서로 어긋날 위험(예: `LLM_PROVIDER=mock`인데 `USE_REAL_BEDROCK=true`)이 생긴다. 하나의 신뢰 가능한 소스(single source of truth)를 유지한다. |
| 2026-08-20 | `color_tags` 필드 최종 제거 | `mood_tags`/`genre_tags` 제거 결정과 함께 처리됐어야 하나 누락되어 있었음. 확인 결과 `color_tags`는 애초에 사용자 요구사항으로 확정된 필드가 아니라, 초기 하네스 문서 세팅 시 README.md의 "무드·색상 다중 필터링" 서술을 참고해 임의로 `mood_tags`와 짝지어 추가한 필드였다. 처음부터 근거 없이 들어간 것이므로 뒤늦게 정정한다. |
| 2026-08-20 | GIN 인덱스 용도를 JSONB/배열 태그 필터링에서 `description + category` 결합 텍스트의 `tsvector` 전문검색(Full-Text Search)으로 전환. 하이브리드(벡터+키워드) 검색은 파라미터로 옵션화하고 기본값은 벡터 단독 검색으로 시작 | `mood_tags`/`genre_tags`를 만들지 않기로 하면서 태그 배열에 대한 GIN 사용 목적이 사라졌다. 대신 `search_vector`(tsvector) 컬럼에 GIN을 걸어 키워드 검색 인프라로 재활용한다. 하이브리드 검색의 실질적 효과가 검증되지 않았으므로 우선 옵션으로만 구현하고, 실사용 로그가 쌓이면 A/B로 벡터 단독 대비 개선 여부를 검증한 뒤 기본값 전환을 재검토한다. |
| 2026-08-20 | `mood_tags` 컬럼 제거, `genre_tags` 컬럼 신설하지 않음. `category`는 파싱 없이 원본 TEXT로 저장하고 임베딩 대상 텍스트에 포함 | Basic API가 보내는 카테고리 데이터가 아직 무드/장르 단위로 정규화되어 있지 않아, 지금 태그 컬럼을 만들면 실제 값 없이 스키마만 앞서가는 추측성 설계가 된다. 원본 텍스트를 그대로 저장하고 임베딩에 포함시키면 RAG 검색 품질에는 기여하면서 정규화 비용은 나중(Basic API 쪽 데이터가 안정된 후)으로 미룰 수 있다. |
| 2026-08-19 | `AGENTS.md`의 origin 주소를 `https://github.com/dont-paw-get/backend-discovery.git`로 정정 | 기존 값이 `backend-book.git`이었다. AGENTS.md가 backend-book 저장소에서 파생되며 남은 값으로, 이 저장소 기준과 불일치해 브랜치·PR 안내가 잘못된 원격을 가리킬 위험이 있었다. |
| 2026-08-19 | 의존성 관리는 `pyproject.toml` + `uv.lock` (uv) 채택, `requirements.txt` 미사용 | AGENTS.md "기술 스택·패키지 구조 변경" 항목이 파이썬 의존성 파일로 `pyproject.toml`과 lock 파일을 지정한다. ruff·mypy·pytest 설정을 한 파일로 모을 수 있어 설정 산재를 막고, uv는 lock 재현성과 설치 속도가 유리하다. |
| 2026-08-19 | `/internal/*` 라우터는 공유 시크릿 헤더(`X-Internal-Token`) 검증을 요구 | 서비스 간 전용 엔드포인트를 무인증 노출하면 외부에서 읽기 모델을 임의 조작할 수 있다. 값은 `.env`로 주입한다. 인프라 확정 후 mTLS/네트워크 격리로 대체할지 재검토한다. |
| 2026-08-19 | 임베딩 차원 1536 결정 | Titan Embed Text v1 가정. 추후 AWS 연동 시 사용할 모델에 따라 마이그레이션 및 재검토 필요. |

| 2026-09-02 | CLIAR-171 배포 후 발견된 Bedrock `ValidationException`(Claude Sonnet 5에서 `temperature`, `top_p` 둘 다 deprecated) 핫픽스를 CLIAR-171 범위 내에서 처리하고(PR #34, #35), 새 버그 티켓을 파지 않는다. 도서 추천 카드(`### 📖`)에도 서재 카드(`### 📚`, CLIAR-196)와 동일한 구조화 필드(`RecommendedBookCard`: title/author/page_count/reason)를 도입하기로 CLIAR-229로 확정한다 | dev 클러스터 로그(`kubectl logs`)로 실측한 결과 `temperature`만 제거하고 `top_p`를 남겼을 때도 동일한 `ValidationException`이 재발함을 확인했다 — Claude Sonnet 5는 두 샘플링 파라미터를 모두 지원하지 않으므로 `max_tokens`만 남기는 것이 유일한 해결책이다. 프론트 "책 등록" 화면에서 저자 필드에 "(548쪽)"이 함께 파싱되는 버그는, 백엔드가 마크다운 텍스트로만 응답하고 구조화 필드를 주지 않아 프론트가 정규식 파싱에 의존하기 때문임을 코드로 확인했다(백엔드에 `<br>` 생성 지점은 없음도 확인 — 프론트 마크다운 렌더러 이슈로 별도 전달). 마크다운 포맷만 줄바꿈으로 분리하는 임시 완화책 대신, CLIAR-196 선례를 따라 구조화 필드를 병행 제공하는 방식을 택한 이유는 LLM 자유 생성 마크다운 포맷 변화에도 프론트 파싱이 깨지지 않는 구조적 해결이기 때문이다 |

| 2026-09-02 | CLIAR-229로 도서 추천 카드(`### 📖`)에도 서재 카드(ADR 0005)와 동일한 구조화 필드(`RecommendedBookCard`: title/author/page_count/reason)를 `ChatResponse.recommended_books`로 병행 제공한다. 마크다운 `message`는 하위 호환을 위해 그대로 유지한다. 스트리밍(`stream_chat`) 경로는 HTTP 헤더가 스트림 시작 전에 확정되는 구조적 제약(ADR 0007 2.2절과 동일)으로 이번 범위에서 `recommended_books`를 제공하지 않고 동기(`chat`) 응답에만 적용한다. 출력 HTML 태그(`<br>` 등) 노출 방어용 `sanitize_html_tags` 순수 함수를 추가한다 | 프론트 "책 등록" 화면 스크린샷으로 "저자" 입력란에 `톰 버틀러 보던 (548쪽)`처럼 쪽수가 함께 들어가는 버그를 확인했다. 코드 확인 결과 백엔드가 구조화 필드 없이 마크다운 텍스트만 제공해 프론트가 `- **저자**: {name} ({page}쪽)` 문자열 전체를 author로 파싱하고 있었다. 마크다운 포맷만 줄바꿈으로 분리하는 임시 완화책(대안 b) 대신 구조화 필드 병행 제공(대안 a)을 택한 이유는, LLM 자유 생성 마크다운 포맷이 조금이라도 바뀌면(예: "쪽"→"페이지") 문자열 파싱 방식은 다시 깨지지만 구조화 필드는 파서(`parse_recommended_books_from_markdown`) 내부에서만 흡수되고 실패 시에도 원본 `message`가 보존되어 사용자 응답 자체는 깨지지 않기 때문이다. `<br>` 노출은 전체 소스 검색으로 백엔드에 생성 지점이 없음을 확인했다(프론트 마크다운 렌더러의 이스케이프 문제로 별도 전달) — 다만 LLM이 HTML을 흉내낼 가능성에 대한 출력단 방어로 `sanitize_html_tags`를 추가했다 |
